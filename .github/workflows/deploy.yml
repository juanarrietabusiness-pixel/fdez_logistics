name: Deploy a GitHub Pages

on:
  push:
    branches:
      - main
      - claude/fdez-logistics-refactor-0d4gha
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

# Una sola publicación a la vez; no cancela un despliegue en curso.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  # Reutiliza el workflow de tests: si algo falla, no se publica nada.
  test:
    name: Tests
    uses: ./.github/workflows/tests.yml

  build:
    name: Preparar sitio
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verificar archivos requeridos
        run: |
          test -f index.html || { echo "::error::Falta index.html"; exit 1; }
          test -f logo.webp  || { echo "::error::Falta logo.webp";  exit 1; }

      - name: Armar carpeta pública
        run: |
          mkdir -p _site
          cp index.html 404.html logo.webp _site/
          if [ -d assets ]; then
            cp -r assets _site/
            rm -f _site/assets/img/README.md
          fi
          touch _site/.nojekyll
          echo "Archivos que se publican:"
          find _site -type f | sed 's|^_site/||' | sort

      # enablement: true activa GitHub Pages en modo "GitHub Actions"
      # automáticamente la primera vez, sin tocar Settings a mano.
      - name: Configurar Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Subir artefacto
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    name: Publicar
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Desplegar a GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Mostrar link de previsualización
        run: |
          {
            echo "### Sitio publicado :rocket:"
            echo ""
            echo "**${{ steps.deployment.outputs.page_url }}**"
          } >> "$GITHUB_STEP_SUMMARY"
